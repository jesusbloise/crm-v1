# =============================================
# CONFIGURACIÓN PARA POSTGRESQL (LOCAL Y PRODUCCIÓN)
# =============================================
# Copia este archivo a .env y configura tus valores

# Puerto del servidor
PORT=4000

# JWT Secret (cambiar en producción con un valor largo y aleatorio)
JWT_SECRET=tu-secreto-jwt-super-seguro-y-largo-aqui

# Tenant por defecto
DEFAULT_TENANT=demo

# =============================================
# POSTGRESQL - DESARROLLO LOCAL
# =============================================
# Asegúrate de tener PostgreSQL instalado y corriendo localmente
# Crea la base de datos: psql -U postgres -c "CREATE DATABASE crm_db;"

PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=crm_db

# =============================================
# POSTGRESQL - PRODUCCIÓN
# =============================================
# En producción, usa DATABASE_URL en lugar de las variables individuales
# Ejemplo: DATABASE_URL=postgresql://user:password@host:5432/dbname
# (Las variables PGHOST, PGUSER, etc. se ignoran si DATABASE_URL existe)

# DATABASE_URL=postgresql://usuario:contraseña@host:5432/nombre_base_datos

# =============================================
# NOTAS IMPORTANTES
# =============================================
# - PostgreSQL debe estar instalado y corriendo localmente
# - Crea la base de datos antes de arrancar: CREATE DATABASE crm_db;
# - Las migraciones se ejecutan automáticamente al iniciar el servidor
# - En Windows: descarga PostgreSQL desde https://www.postgresql.org/download/windows/
# - Ver guía completa en: ../POSTGRESQL-SETUP.md
